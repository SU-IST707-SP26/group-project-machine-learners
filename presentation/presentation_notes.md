Improvements:
1. Non-Finbert semantic features
2. Split training groups by modes (predict within modes)
3. Add additional structural features (ex. sentence count, section length)
4. Include additional embeddings and reduce dimensions
